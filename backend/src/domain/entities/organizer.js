export class Organizer{
    constructor({ id_organizer,name,email,password_hash }){
        this.id_organizer = id_organizer
        this.name = name
        this.email = email
        this.password_hash = password_hash
    }
}